<%= form_for(@bird_list) do |f| %>
  <% if @bird_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bird_list.errors.count, "error") %> prohibited this bird list from being saved:</h2>

      <ul>
      <% @bird_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
